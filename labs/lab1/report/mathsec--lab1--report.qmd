---
## Author
author:
  name: Тазаева Анастасия Анатольевна
  degrees: BSc
  orcid: 
  email: 1032259385@pfur.ru
  affiliation:
    - name: Российский университет дружбы народов
      country: Российская Федерация
      postal-code: 117198
      city: Москва
      address: ул. Миклухо-Маклая, д. 6

## Title
title: "Лабораторная работа №1"
subtitle: "Шифры простой замены"
license: "CC BY"
---

# Цель работы

Ознакомиться с шифрами простой замены. Реализовать их.

# Задание

Реализовать на языке программирования Julia:

1. Шифр Цезаря.
2. Шифр Атбаш.

# Теоретическое введение

Шифры простой замены представляют собой класс криптографических методов, в основе которых лежит принцип замены символов исходного текста на другие символы по определенному алгоритму. При этом каждому символу открытого текста соответствует единственный символ шифртекста.

## Виды шифров простой замены.

В рамках данной лабораторной работы рассматриваются два основных типа:

- Шифр Цезаря - моноалфавитная подстановка с фиксированным сдвигом букв алфавита;
- Шифр Атбаш - метод зеркальной замены символов алфавита.


# Выполнение лабораторной работы

## Шифр Цезаря

Написан программный код, реализующий шифр Цезаря:

```Julia
function caesar_cipher_encrypt(text::String, shift_value::Int)
 result = ""
 for char in text
   if isletter(char) 
     if (1039 < Int(char) < 1104)
       base = isuppercase(char) ? codepoint('А') : codepoint('а')
       encrypted_char = Char(base + mod((Int(char) - base + shift_value), 32))
     else
       base = isuppercase(char) ? codepoint('A') : codepoint('a')
       encrypted_char = Char(base + mod((Int(char) - base + shift_value), 26))
     end
     result *= encrypted_char
   else
     result *= " "
   end
 end
 return result
end

function caesar_cipher_decrypt(text::String, shift_value::Int) 
 if (1039 < Int(text[1]) < 1104)
   return caesar_cipher_encrypt(text, 32 - shift_value)
 else
   return caesar_cipher_encrypt(text, 26 - shift_value)
 end
end
```

Получен следующий результат выполнения программного кода ([рис. @fig-001]).

![Шифр Цезаря. Примеры отработки](image/caesar.png){#fig-001 width=70%}

## Шифр Атбаш

Написан программный код, реализующий шифр Атбаш:

```Julia
function atbash_encrypt(text::String)
    result = ""
    for char in text
        if 'А' <= char <= 'Я'  
            new_char = Char('Я' - (char - 'А'))
            result *= new_char
        elseif 'а' <= char <= 'я' 
            new_char = Char('я' - (char - 'а'))
            result *= new_char
        elseif 'A' <= char <= 'Z'  
            new_char = Char('Z' - (char - 'A'))
            result *= new_char
        elseif 'a' <= char <= 'z' 
            new_char = Char('z' - (char - 'a'))
            result *= new_char
        else
            result *= char
        end
    end
    return result
end
```

Получен следующий результат выполнения программного кода ([рис. @fig-002]).

![Шифр Атбаша. Примеры отработки](image/atbash.png){#fig-002 width=70%}

# Выводы

В ходе лабораторной работы были изучены и реализованы шифры простой замены:

- шифр Цезаря,
- шифр Атбаш.

# Список литературы{.unnumbered}

::: {#refs}
:::
